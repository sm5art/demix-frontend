import React from 'react';

import theme from '../theme';

const Jumbotron = () => (
    <div>
        <h1 style={{...theme.fonts.h1, marginTop: theme.spacing.large, marginBottom: theme.spacing.large}}>Seperate vocals from music.</h1>
    </div>
);

export default Jumbotron;